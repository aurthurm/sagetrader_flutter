import 'package:flutter/material.dart';

class MSPTWebLinkPage extends StatelessWidget {
  const MSPTWebLinkPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("MSPT Web Link Page"),
      ),
    );
  }
}
