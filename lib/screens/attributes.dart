import 'package:flutter/material.dart';

class AtrributesPage extends StatelessWidget {
  const AtrributesPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Atrributes Page"),
      ),
    );
  }
}
