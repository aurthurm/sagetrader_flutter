import 'package:flutter/material.dart';

class CalculatorsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Trading Calculators"),
      ),
    );
  }
}
